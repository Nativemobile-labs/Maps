import { StyleSheet, Text, View } from 'react-native'
import React from 'react';
import NavigateRoutes from './src/navigateRoutes/NavigateRoutes';
const App = () => {
  return (
    <NavigateRoutes />
  )
}

export default App

const styles = StyleSheet.create({})